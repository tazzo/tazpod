FROM devpod-k8s:local

# --- LAYER 3: AI POWERED ---
# Contains the heavy Gemini CLI.

USER vscode

# Install Gemini CLI (Globally via NPM)
RUN . "$NVM_DIR/nvm.sh" && \
    echo "Installing Gemini CLI (this might take a while)..." && \
    npm install -g @google/gemini-cli@latest
