secrets/
/tazpod
.tazpod-vault/
.tazpod/vault/vault.img
.tazpod/vault/
.tazpod/.gemini/
.tazpod/.opencode/
bin/
tazpod-lxc
*.tfstate
*.tfstate.backup
.terraform/
# LXC runtime build logs: they capture provisioning output (and with it anything a
# task prints) and must never be committed.
runtimes/lxc/logs/
